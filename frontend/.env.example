# Local dev: không cần set, mặc định trỏ về http://localhost:3000
# Deploy: set biến này trên Vercel/Netlify (Project Settings -> Environment Variables)
# trỏ tới URL backend đã deploy, ví dụ: https://ar-backend.onrender.com
VITE_API_URL=http://localhost:3000
